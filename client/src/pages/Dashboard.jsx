// Import library
import React from "react";

// Import components

const Dashboard = () => {
    return (
        <section className="section">
            <div className="container">
                <h1>Dashboard</h1>
            </div>
        </section>
    );
};

export default Dashboard;
